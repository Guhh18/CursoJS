const nomes = ["João", "Maria", "Lucy", "Gustavo"]

for(let i = 0; i < nomes.length; i++) {
    console.log(`nome ${i} é: ${nomes[i]}`);
}

// Nesse exemplo usamos um laço for pra mostrar os valores/nomes de um array